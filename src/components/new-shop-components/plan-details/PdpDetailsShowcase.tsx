// Backward compatibility shim – delegate default export to core implementation.
export { default } from './core/PdpDetailsShowcase';
// Some bundlers can be sensitive; ensure no additional exports.
