// INTEGRATION INSTRUCTIONS
// 1. Import the modal at the top of your page:
// import { MedigapQuoteDetailsModal } from "./MedigapQuoteDetailsModal";

// 2. Add state to manage the selected quote and modal visibility:
// const [selectedQuote, setSelectedQuote] = useState<any | null>(null);
// const [isDetailsOpen, setIsDetailsOpen] = useState(false);

// 3. Update the onViewDetails and onSelectQuote handlers in <MedigapQuoteTable>:
// onViewDetails={(id: string) => {
//   const quote = medigapQuotes.find(q => q.id === id);
//   setSelectedQuote(quote);
//   setIsDetailsOpen(true);
// }}
// onSelectQuote={(id: string) => {
//   // You can use this for selection logic, or also show details
//   const quote = medigapQuotes.find(q => q.id === id);
//   setSelectedQuote(quote);
//   setIsDetailsOpen(true);
// }}

// 4. Render the modal in your JSX:
// <MedigapQuoteDetailsModal open={isDetailsOpen} onClose={() => setIsDetailsOpen(false)} quote={selectedQuote} />

// This will make the View Details and Select Quote buttons work as expected.
